# Dependencies and build output (not our source).
vendor/
node_modules/
build/

# Generated wire-contract types (regenerated from the served JSON Schema).
src/Contract/WireDataset.php
src/Contract/CatalogResponse.php
src/Contract/WellKnownDoc.php

# E2E screenshot harness (Playwright + wp-env fixtures). Dev-only scaffolding
# that never ships (see .distignore) and is excluded from PHPCS too. Its fixture
# mu-plugin deliberately serves raw SVG and short-circuits the read API — test
# constructs, not a production/runtime surface.
tests/e2e/
